# Checkpoints

The release contains one seed-43 checkpoint for each dataset and target:

- `porto_netdtw.pt`
- `porto_tp.pt`
- `chengdu_netdtw.pt`
- `chengdu_tp.pt`

Each checkpoint is a PyTorch state dictionary containing the frozen landmark
table and the trained trajectory encoder.

